@page
@using PokeRepo.Enum
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}



<a class="btn btn-primary" asp-page="PokeRepo/index">PokeRepo</a>

@foreach(var pokemon in Enum.GetValues(typeof(Pokemons)))
{
    <a asp-page="/Details">
    <h3>@pokemon</h3>
    </a>
};